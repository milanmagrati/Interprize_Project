"""
Intentionally empty for now.

The frontend is driven entirely by `core/sample_data.py`. When the real
backend lands, define Category / Package / Testimonial / City / FAQ models
here with the same field names used in the sample dicts, then swap the
sample_data lookups inside views.py for querysets. See README.md.
"""

from django.db import models  # noqa: F401
